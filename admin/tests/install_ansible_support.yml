---
- name: test install dmidecode required by ansible gather_facts
  hosts: all:!unmanaged_systems
  become: true
  tasks:
    - name: test install dmidcode support
      debug:
        msg: TODO implement


- name: test install required packages for ansible
  hosts: localhost
  gather_facts: false
  run_once: true
  tasks:
    - debug:
        msg: TODO implement
