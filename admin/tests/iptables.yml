---
- name: test initialize iptables
  hosts: all:!unmanaged_systems:!localhost
  become: true
  tasks:
    - include_tasks: tasks/test_pkg_installed.yml
      vars:
        pkg: "{{ 'iptables-services' if ansible_distribution == 'CentOS' else 'iptables' }}"
