---
- import_playbook: mk_storage_resources.yml


- name: test Configure IES for PIRE
  hosts: ies
  become: true
  become_user: irods
  run_once: true
  tasks:
    - debug:
        msg: TODO implement
