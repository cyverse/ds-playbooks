---
- name: verify that all servers are up
  shell: |
    if ! status="$(/var/lib/irods/irodsctl status)"; then
      exit 1
    fi
    grep --invert --quiet 'No servers running' <<< "$status"
  changed_when: false
