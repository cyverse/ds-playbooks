---
- name: check correct {{ pkg }} package installed
  yum:
    list: "{{ pkg }}"
  register: result
  failed_when: |
    result.results | selectattr("yumstate", 'equalto', "installed") | list | count != 1 or
    version is defined and result.results[0].version is version_compare(version, '!=')
