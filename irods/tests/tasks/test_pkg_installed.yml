---
- name: check correct {{ pkg }} package installed
  yum:
    list: "{{ pkg }}"
  register: result
  failed_when: |
    result.results is not defined or
    result.results|count != 1 or
    result.results[0].version is version_compare(version, '!=')
