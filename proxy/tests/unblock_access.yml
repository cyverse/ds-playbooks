---
- name: test restore all client access
  hosts: localhost
  gather_facts: false
  tasks:
    - name: "test haproxy : firewall | disable conntrack for proxied connections"
      debug:
        msg: TODO verify {{ item }}
      with_items:
        - webdav clients
        - webdav hosts
        - irods clients
        - irods hosts

    - name: "test haproxy : firewall | open for relevant connections"
      debug:
        msg: TODO implement
